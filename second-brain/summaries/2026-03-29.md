# 2026-03-29 Daily Wrap Up

## Decisions（确定的决策）
- 记忆系统主槽已切换到 `memory-lancedb-pro`，并完成基础可用性验证。
- `memory_recall` 工具路径缺省 scope 回退逻辑已修复：无 runtime agentId 时默认落到 `agent:main`。
- `memory-pro` CLI 初始化流程已补强：FTS 状态读取与 `reindex-fts` 初始化顺序已修复。
- 汕头大学刷课问题上，Kent 明确要求：不要持续把问题抛回给他，应直接按最优方式处理。
- 对“真看视频自动连播”方案，已放弃 AutoHotkey 坐标点击作为主路线，转向更稳的浏览器自动化/脚本方案。

## Action Items（待办事项）
- 继续定位 `memory-lancedb-pro` 的真实检索链路，确认 BM25 / fused / rerank 是否在实际搜索中生效。
- 排查 `龙旗科技 持仓` 等记忆条目为何在 CLI / tool path 中召回不稳定。
- 为学习平台视频连播补一套可真正落地的自动化方案（优先非坐标点击）。
- 处理 Playwright 运行依赖缺失问题（当前缺 `libnspr4.so` 等库）。

## Key Conversations（关键对话摘要）
- 飞书里用户确认“你对应的 agent 是哪个”，主会话明确答复：当前对应 `main agent`，主作用域为 `agent:main`。
- finance agent 在飞书中自报身份为 `finance`，说明多 agent 已能分别响应。
- study agent 围绕汕头大学继续教育刷课做了长轮次探索：
  - 用户希望实现“真看视频自动连播”，而非仅伪造进度；
  - 已明确平台行为：视频播完会弹练习题弹窗；按 ESC 后回到已播完的视频页，不会自动进入下一节；
  - “查看所有章节”按钮在右上角，但基于屏幕坐标的 AutoHotkey 方案不稳定；
  - 用户最终要求不要再反复追问，直接选择最优技术路线解决。

## Technical Notes（技术笔记/踩坑）
- Playwright 在当前 WSL/Linux 环境可安装，但实际启动失败，报错缺失共享库：`libnspr4.so`。
- 浏览器控制工具当前不可用：`browser` 工具返回 “No supported browser found”。
- 当前学习平台已有两条路线：
  - `progress_updater.py`：直接伪造学习进度，可快速刷到完成；
  - AHK/浏览器真实播放：用于“真看视频”，但现阶段自动连播稳定性不足。
- P10001 应用数学1 视频 URL 规律已确认：`jjwikiid` 从 `21751` 开始递增，共约 25 节。

## Pending（未完成/待续）
- 学习平台“真看视频自动连播”仍未彻底跑通。
- `memory-lancedb-pro` 检索链路真实性验证未完成。
- Playwright 方案尚未完成依赖修复与实测。

## Tomorrow（明天要做的事）
- 优先修好学习平台自动连播的最终方案，尽量避免坐标点击。
- 补齐 Playwright/浏览器运行环境依赖，再重试脚本自动化。
- 继续验证 memory-pro 的混合检索与 rerank 是否真正参与召回。