# 2026-03-29 日志

> 由 HEARTBEAT.md 定时任务 MICRO_SYNC + DAILY_WRAPUP 生成（2026-03-30 05:55 CST 执行）

## MICRO_SYNC（最近 3 小时）
- 原因：Kent 希望汕头大学学习平台实现“真看视频自动连播”，不要每节课手点。
- 发现：
  - 视频播完会弹练习题弹窗；
  - 按 ESC 可关闭弹窗，但只会回到当前已播完的视频播放页，不会自动进入下一节；
  - “查看所有章节”按钮位于右上角，但基于 AutoHotkey 坐标点击的方案不稳定；
  - P10001 应用数学1 视频 URL 规律已确认：`jjwikiid=21751...21775` 递增；
  - 用户明确要求：不要把问题反复抛回给他，应直接选最优方案处理。
- 结论：
  - AHK 坐标点击不是稳定路线；
  - 后续应优先转向 URL 驱动 / 浏览器自动化（Playwright 等）方案；
  - 但当前 Playwright 在本机 WSL 环境又被缺少 `libnspr4.so` 阻塞，仍待修复。

## DAILY_WRAPUP 摘要
### 决策
- memory slot 已切换到 `memory-lancedb-pro` 并通过基础验证。
- `memory_recall` 工具默认 scope 回退到 `agent:main` 的问题已修复。
- `memory-pro` CLI 的 FTS 初始化问题已修复。
- 学习平台自动连播将放弃以坐标点击为主的方案。

### 关键进展
- 主会话明确识别为 `main agent`。
- finance agent 能独立在飞书中响应。
- study agent 长轮次排查了学习平台“真看视频自动连播”问题。

### 技术备注
- `memory-lancedb-pro` 仍未证实 BM25 / fused / rerank 在真实检索链路中生效。
- Playwright 当前失败原因：运行时缺失 `libnspr4.so`。
- `browser` 工具当前不可用，无法直接控制浏览器。

### 待续
- 修复 Playwright 依赖并重试浏览器自动化。
- 继续定位 memory-pro 召回链路异常。
